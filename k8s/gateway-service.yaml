apiVersion: v1
kind: Service
metadata:
  name: likes-gateway
  labels:
    app: likes-app
spec:
  selector:
    app: likes-app
    component: gateway
  ports:
  - port: 8154
    targetPort: 8154
    protocol: TCP
  type: LoadBalancer 